class Entity {

	/**
		Store width and height in grid units
	**/
	constructor(width, height, depth, scene, assetsManager) {
		this._width = width;
		this._height = height;
		this._depth = depth;
		this._scene = scene;
		this._assetsManager = assetsManager;
	}

}
